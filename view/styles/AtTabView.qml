import QtQuick.Controls 1.4

TabView {
    property var $tabPosition : Qt.BottomEdge
    property int $width : 800
    property int $height : 400

    tabPosition : $tabPosition
    //width : $width
    //height : $height
}
